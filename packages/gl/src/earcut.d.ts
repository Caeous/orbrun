/** mapbox/earcut 3.0.2, vendored: triangulates a flat list of 2D points with holes into index triples. */
export default function earcut(data: ArrayLike<number>, holeIndices?: number[] | null, dim?: number): number[]
